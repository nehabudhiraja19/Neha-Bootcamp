// help to wrap up components as we cannot wrap div and main separately instead of passing as 
// an array we will wrap it//


//chuld of layout.js//

const aux=(props)=> props.children;


export default aux;